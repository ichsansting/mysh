#!/usr/bin/env fish
set -x MISE_DATA_DIR "$HOME/.mysh/mise"
if test "$argv[1]" = "compose"
    set argv $argv[2..]
    exec mise x docker-compose@latest -- docker-compose $argv
end
exec mise x docker-cli@latest -- docker $argv
